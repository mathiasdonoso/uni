[project]
name = "uni"
version = "0.1.0"
description = "A personal cli tool to install packages across different Linux distributions"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
uni = "uni.main:main"
